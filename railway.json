{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "pip install -r requirements.txt && python -m uvicorn backend.main:app --host 0.0.0.0 --port $PORT",
    "restartPolicy": "ON_FAILURE"
  }
}